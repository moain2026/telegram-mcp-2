# telegram-mcp configuration — copy to .env and fill in (NEVER commit .env)
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
# Portable session string (generate with login.py). Keeps you logged in.
TELEGRAM_STRING_SESSION=
# Where downloaded media is saved (optional)
TELEGRAM_DOWNLOAD_DIR=./downloads
